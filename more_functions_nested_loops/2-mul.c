#include "main.h"
#include <stdio.h>

/**
* mul - function to multiplication
* @a: defines first parameter
* @b: defines second parameter
* Return: Always 0.
*/

int mul(int a, int b)
{
return (a * b);
}
