#include <stdio.h>
#include "main.h"

/**
* _islower - prints 0 if letter is uppercase, else 1
* @c: represents letter
* Return: Always 0 (Success)
*
*/

int _abs(int c)
{
if (c < 0)
{
return (c);
}
return (-c);
}
