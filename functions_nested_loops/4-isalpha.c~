#include <stdio.h>
#include "main.h"

/**
* _islower - prints 0 if letter is uppercase, else 1
* @c: represents letter
* Return: Always 0 (Success)
*
*/

int _isalpha(int c)
{
if ((c >= 'a' || c >= 'A') && (c <= 'z' || c <= 'Z'))
return (1);
else
return (0);
}
